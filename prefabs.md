# Prefabs — Speed Runner Pro

عناصر جاهزة قابلة لإعادة الاستخدام (موثقة هنا، منفذة داخل game.js)

## 🧱 Obstacle Variants

```
normal     → 52×64px  — عقبة أرضية عادية
tall       → 44×96px  — عقبة طويلة (تتطلب الانزلاق أو القفز العالي)
flying     → 52×40px  — عقبة طائرة (تتطلب الانزلاق فقط)
small      → 36×36px  — عقبة صغيرة (سهلة القفز)
```

## 🪙 Coin Patterns

```
line      → صف أفقي من العملات
arc       → قوس نصف دائري
cluster   → تجمع عشوائي
```

## ⚡ PowerUp Types

```
shield     → درع يمتص ضربة واحدة (8 ثانية)
slowmo     → تبطيء الزمن ×0.35 (8 ثانية)
magnet     → جذب العملات في نطاق 180px (8 ثانية)
doubleJump → قفزة مزدوجة في الهواء (8 ثانية)
```

## 🎭 Characters

```
boy   → متاح افتراضياً
girl  → يُفتح بـ 500 عملة من المتجر (hue-rotate للتمييز)
```
